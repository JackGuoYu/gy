server:
  port: 9002

spring:
  application:
    name: gy-config  # 应用名称
  cloud:
    config:
      server:
        git:
          uri:  https://github.com/JackGuoYu/gy-config.git  #配置文件所在仓库
          username: JackGuoYu  #登录账号
          password: gy7343733106  #登录密码
          default-label: master #配置文件分支
          search-paths: config-repo  #配置文件所在根目录
#    bus:
#      refresh:
#        enabled: true
#  kafka:
#    bootstrap-servers: 39.101.179.31:9092
#    consumer:
#      group-id: springCloud-bus

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:9001/eureka/

management:
  endpoints:
    web:
      exposure:
        include: "*"

